1. Colab에서 opencv기반으로, pretrained 된 faster CNN을 된 tensorflow 모델을 불러와서 detecting 시행
2. Colab에서 tensorflow 기반으로,  pretrained 된 faster CNN을 tensorflow 모델을 불러와서 detecting 시행(GPU적용 개빠름)
3. CVAT 프로그램을 이용해서 원본에 라벨마스크를 씌운 후 mask RCNN annotation을 적용해서 저장하는 방법까지 학습